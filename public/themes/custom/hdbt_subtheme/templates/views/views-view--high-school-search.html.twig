{{ attach_library('hdbt_subtheme/high-school-search') }}

{% embed '@hdbt/component/content-list-with-count.twig' with { show_count_container: false, tabs: true }%}
  {% block tabs_count_container %}
    {%- if total_rows -%}
      {{ total_rows }} {% trans with {'context': 'High school search count'}%}general upper secondary school{% plural total_rows %}general upper secondary schools{% endtrans %}
    {%- else -%}
      {{ 'No results'|t({}, {'context' : 'Content list with count no results title'}) }}
    {%- endif -%}
  {% endblock tabs_count_container %}
{% endembed %}
